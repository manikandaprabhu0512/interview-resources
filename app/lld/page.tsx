import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Low Level Design",
};

const page = () => {
  return (
    <div>
      <div>What is LLD?</div>
    </div>
  );
};

export default page;
